# Create this variable in Vercel → Project Settings → Environment Variables.
# Do not put the key in client-side code or commit a real key to Git.
OPENAI_API_KEY=

# Optional: use a different vision-capable model.
# OPENAI_VISION_MODEL=gpt-4.1-mini
